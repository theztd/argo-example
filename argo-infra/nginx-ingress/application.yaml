---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-ingress
  namespace: argocd
spec:
  project: infra
  source:
    repoURL: https://github.com/theztd/argo-example
    targetRevision: main
    path: argo-infra/nginx-ingress/chart
    helm:
      parameters:
        - name: namespace
          value: nginx-ingress
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: nginx-ingress
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
